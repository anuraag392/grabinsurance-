# GrabInsurance – Environment Variables Template
# Copy this file to .env and fill in your values.

# No external AI API key required.
# Offer copy is generated by the built-in mock AI generator (no paid APIs).

# SQLite database path (default: ../data/grabinsurance.db)
# DATABASE_URL=sqlite:///./data/grabinsurance.db
